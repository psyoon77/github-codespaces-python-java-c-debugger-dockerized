notes

zoom screen share - administrator access, windows os level notifications, etc are not accessible

teamviewer screen share - administrator access, windows os level notifications, etc are accessible -
    i download teamviewer for mac - login with yoon.cs.tutor -
    download teamviewer for linux -
    i click sessions -  new sessions - share the session id -
    enter the session id from teamviewer

screen captrue share on windows -
    win + shift + s -
    control + v at the zoom chat

copy and paste -
    from zoom windows on my mac - use control + c or v -
    from my mac - use command + c or v

--------------------------------------------

onboarding linux

0 zoom install
wget https://zoom.us/client/latest/zoom_amd64.deb
sudo apt install ./zoom_amd64.deb
(double check url and file name)
1 i share a zoom link through zoom chat
2 enter the zoom room
3 share screen
4 share mouse control and clip board
5 chrome install
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo apt install ./google-chrome-stable_current_amd64.deb
(double check url and file name)
6 teamviewer setup - manual at the top
wget https://download.teamviewer.com/download/linux/teamviewer_amd64.deb
sudo apt install ./teamviewer_amd64.deb
(double check url and file name)
7 terminal open
8 apt update and upgrade
sudo apt update
sudo apt upgrade -y
9 apt install
sudo apt install -y git vim curl wget openssh-client ca-certificates gnupg
9 install github gh
type -p curl >/dev/null || sudo apt install -y curl
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
sudo apt update
sudo apt install -y gh
9 install docker, etc
for pkg in docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc; do sudo apt remove -y $pkg; done
sudo apt install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
1 installation integrity check
apt --version
git --version
gh --version
vim --version
curl --version
wget --version
ssh -V
code --version
docker --version
docker compose version
8 create a folder at an adequate directory - pwd ls cd mkdir rmdir rm
9 github clone onboarding from the folder
cd [folder path]
git clone https://github.com/cs-yoon-ai/onboarding-cross
1 docker start
docker desktop start
docker desktop restart
docker desktop status

11 vs-code open the cloned onboarding-cross folder
(command (or control) + shift + n - new vs-code window open)
13 vs-code extensions local install - command (or control) + shift + x
docker
dev containers
container tools
live share
remote development
4 command (or control) + shift + p
5 dev containers: reopen in container
6 vs code will reopen using the container - click ⓘ connecting to dev container (show log)
7 note that all python, c, and java are managed by conda
8 docker container inside test
pwd
ls
conda info --envs
which python
python --version
which javac
javac -version
which gcc
gcc --version
which gdb
gdb --version
git --version
gh --version
apt install vim
pip install requests
wget https://www.google.com
curl https://www.github.com

1 s-code - file - autosave - click
8 command (or control) + shift + p
9 type live share: start collaboration session (share)
10 sign in when prompted
12 command (or control) + shift + p
13 type live share: copy collaboration link
14 share the link through zoom chat
15 chrome paste url
15 open in visual studio desktop
10 command (or control) + shift + p
11 type live share: share terminal - read write

(do the following through shared screen teamviewer or zoom)
python program test
0 open a python file
1 command (or control) + shift + p
2 python: select interpreter
3 choose python 3.11.15 (cs-project-docker) /opt/conda/envs/cs-project-docker/bin/python
4 run python click the ▷ button at the top right
5 debug pyton - set break points - command (or control) + shift + d -
    click run and debug -
    select appropriately

ollama llm test
/ai/chat-ollama-openai.py - manual at the bottom of the python program

java program test
0 open a java file
1 let vs-code automatically load java extension specified at .devcontainer/devcontainer.json
2 run java at the top right ▷
3 debug java at the top right ▷ - set breakpoints

c program test
0 open a c file - compile and run with -g for debugging
1 from the root of the project
mkdir -p c/c-executables
2 gcc -g c/hello-world-for.c -o c/c-executables/hello-world-for
./c/c-executables/hello-world-for
3 debug - set breakpoints -
       command (or control) + shift + d -
       run and debug ▷ c/c++:... ∨ - click ∨ -
       choose c/c++:gcc build and debug active file -
       click run and debug ▷

github-git
9 github setup
/manuals/github-git/github-ssh-key/github-ssh-key-linux.txt
1 git initial commit or continuous development
/manuals/github-git/github-git-workflows-cross.txt

9 vs-code - to rebuild docker
10 command (or control) + shift + p
11 dev containers: rebuild container

